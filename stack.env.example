# Variabili override per ambienti di deploy (docker swarm / compose --env-file)
# Esempio: valori diversi da sviluppo per produzione
#APP_USER=admin
#APP_PASSWORD=changeme
#PORT=5000

# Configurazione Docker per produzione
#IMAGE_REGISTRY=silviosanto6605
#IMAGE_TAG=latest
